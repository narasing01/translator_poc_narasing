{# Sample Jinja2 Document - Project Description #}
{# Short overview of a template translation service #}

# {{ project_name | default("My Translation Service") }}

> Version **{{ version | default("0.1.0") }}** — {{ build_date | default("2026-03-26") }}

## What Is This?

A web service that translates **Jinja2 templates**
from {{ source_lang | default("English") }} to {{ target_lang | default("Japanese") }}
while preserving every piece of template syntax intact.

---

## Key Features

{% set features = [
    "Multi-LLM support via configurable providers",
    "Parallel file translation via asyncio",
    "LLM-as-Judge validation with automatic retry",
    "Tone presets for formality control (enterprise, government, casual)",
    "Version control integration — fetch, translate, commit, and publish"
] %}

{% for feat in features %}
- {{ feat }}
{% endfor %}

---

## Architecture at a Glance

```
┌──────────┐      ┌────────────┐      ┌───────────┐
│ Frontend  │─────▶│  API        │─────▶│  LLM API  │
│   (SPA)   │      │  Backend    │      │ ({{ llm_provider | default("LLM") }}) │
└──────────┘      └─────┬──────┘      └───────────┘
                        │
                   ┌────▼─────┐
                   │   VCS     │
                   │   Repos   │
                   └──────────┘
```

Backend URL: `{{ backend_url | default("http://localhost:8000") }}`

---

## Translation Pipeline

The pipeline has **{{ pipeline_phase_count | default(4) }}** phases:

| Phase | Name       | Description                             |
|-------|------------|-----------------------------------------|
| 1     | Extract    | Pull context, glossary, and chapter info|
| 2     | Render     | Map rendered docs to source templates   |
| 3     | Translate  | LLM translates each file in parallel    |
| 4     | Validate   | LLM-as-Judge scores the translations    |

{% if extra_phases is defined %}
{% for p in extra_phases %}
| {{ p.num }} | {{ p.name }} | {{ p.desc }} |
{% endfor %}
{% endif %}

---

## Quick Start

```bash
# Clone and start
git clone {{ repo_url | default("https://example.com/your-org/your-repo.git") }}
cd your-repo

# Backend
cd backend
pip install -e ".[dev]"
python -m uvicorn main:app --reload

# Frontend
cd ../frontend
npm install
npm start
```

API docs available at `{{ backend_url | default("http://localhost:8000") }}/docs`.

---

## Tech Stack

{% set stack = {
    "Backend": "Python, Web Framework, LangChain",
    "Frontend": "SPA Framework, UI Library",
    "LLMs": "Configurable LLM Providers",
    "Infra": "Docker Compose, VCS API"
} %}

| Layer    | Technologies |
|----------|-------------|
{% for layer, tech in stack.items() %}
| {{ layer }} | {{ tech }} |
{% endfor %}

---

## Configuration

Key settings are managed via environment variables:

| Variable             | Default                        | Purpose                  |
|----------------------|--------------------------------|--------------------------|
| `APP_NAME`           | {{ app_name | default("My Translation Service") }} | Display name |
| `VCS_TOKEN`          | —                              | Version control API access |
| `DEFAULT_LLM`        | {{ default_llm | default("your-provider:your-model") }} | Default model |

{% if custom_vars is defined %}
{% for v in custom_vars %}
| `{{ v.name }}` | {{ v.default }} | {{ v.purpose }} |
{% endfor %}
{% endif %}

---

*Generated by {{ generator | default("Sample Pipeline") }}*

{# End of project description document #}
